// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  email     String @unique
  password  String
}

// instalar prisma ORM => npm i -D prisma
// iniciar prisma => npx prisma init
// após criar o sChema, rodar o comando => npx prisma db push
// instalar prisma client => npm install @prisma/client
// para abrir ide prisma => npx prisma studio